---
name: print-sheets
description: Аркуші розкладки 1:1 для сортування й склеювання друкованого набору (print3d-parts/sheets/ → SHEETS.pdf) — контури деталей на A4 у масштабі 1:1, рендери вузлів з виносками до файлів, картки кроків склеювання. Використовуй, коли користувач говорить про аркуші, розкладку, сортування надрукованих деталей, «покласти на контур», виноски, картки кроків, SHEETS.pdf, «як відрізнити втулки/пальці», а також після будь-якої зміни parts.tsv, assembly.tsv чи STL набору — аркуші треба перезібрати.
---

# Аркуші розкладки 1:1 (`print3d-parts/sheets/`)

Єдиний документ для склеювання набору (стара текстова інструкція `ASSEMBLY` — в архіві
`_deprecated/`): папір проти купи схожих деталей: деталь кладеться на свій контур 1:1 — збіглася,
значить вона. Формат користувачеві подобається; розвивати саме його.

```
tools/.venv/bin/python print3d-parts/sheets/make_sheets.py --png      # усе: SHEETS.pdf + sheets.json + png/ (≈1.5 хв)
… --only 3 --no-steps --no-pdf --png                                    # один аркуш без кроків — для ітерацій (≈10 с)
… --fresh                                                                # перерендерити всі PNG (кеш у $TMPDIR/p3d_sheets)
```
`print3d-parts/make.sh` запускає генератор сам, після STL і перед `stl/group.sh`.

| Файл | Що це |
|---|---|
| `make_sheets.py` | генератор: комірки, пакування, рендери, виноски, HTML → PDF; силуети — `tools/stl_mesh.py`, розміри — `tools/caliper_dims.py`, виноски — `tools/hue_callouts.py` |
| `sheets.scad` | обгортка рендерів: підключає `../parts.scad` (а той — модель як бібліотеку), фарбує деталі |
| `SHEETS.pdf` | 6 аркушів розкладки + сторінки кроків (≈21 стор.) |
| `sheets.json` | **усі числа**: комірки, ракурси, пікселі кожної деталі, виноски, кроки — читати його, не PDF |
| `png/N_slug.png` | прев'ю аркушів розкладки з САМОГО PDF (`pdftoppm`, сторінка знаходиться за текстом шапки через `pdftotext`); без poppler — знімок HTML. Латинські назви, бо на них посилаються README |

## Що вирішено (не перевідкривати)

- **Аркуш = вузол з `assembly.tsv`** («1. Стріла» … «7. Складання на пальцях»); склад,
  порядок і тексти «куди/як» беруться звідти, назви файлів і кількості — з `parts.tsv`.
  Колона й циліндри — на одному аркуші, коромисло й тяга — на одному. Нову деталь
  досить вписати в обидві TSV — на аркуш вона потрапить сама.
- **Одна комірка = один файл.** Однакові плоскі деталі (`×N стосом`) — один контур,
  кладуться одна на одну; пальці — кожен окремо (вони й так по одному на файл).
- **Як лежить деталь** — так, як друкується (орієнтація з `parts.tsv` уже кладе
  найбільшу грань на стіл), крім пальців: друкуються стоячи, на папері лежать.
  **Обичайку ковша користувач хоче боком, профілем** — дном вона дає лише
  прямокутник. Не «покращувати» на найбільшу грань.
- **Підписи в комірці**: номер кроку + файл (+`×N`), коротка назва, h (висота над
  папером), отвори (великі — Ø просто всередині отвору, малі — в рядку), для кілець
  Ø/Ø·h, для пальців Ø×L і голівка. Пари з однаковими габаритами (±0.3 мм) отримують
  `= файл (однакові)`, з різницею < 1 мм — `≈ файл: чим різняться`; ліва й права
  (ключі `…_L`/`…_R`, зараз лише боковини ковша — розділені заради логотипа назовні) —
  `дзеркальна до файл`: контури віддзеркалені, кожна кладеться лише на свій. Рядок «куди»
  з комірки прибрано — він на картці кроку, а місце на аркуші дорожче.
- **Розміри на кресленні — лише ті, що знімаються штангенциркулем** (`tools/caliper_dims.py`,
  вимога користувача 2026-09-24). Габарит за осями сторінки (bbox) НЕ пишеться і не
  малюється: повернуту чи клиноподібну деталь так не зміряти. Типи: `flats` між
  паралельними кромками, `flat` від кромки до найдальшої точки по перпендикуляру,
  `edge` довжина прямої кромки (найдовша — завжди), `end` ширина заокругленого кінця,
  `length` найбільша протяжність (ставиться понад ліміт, якщо не діагональ і не дубль
  плоского), `humps` між горбами. Кандидати ранжуються, дублі за значенням (±1 %)
  викидаються, розмір, що вилазить за габарит деталі на > 12 % (похила лінія на
  довгій трубі), не ставиться — і тоді наступний за оцінкою займає його місце. Ліміт
  3–5 розмірів за розміром деталі. Перевіряти відбір: `tools/.venv/bin/python
  tools/caliper_dims.py файл.stl` (у друкованій орієнтації) або `dims` у `sheets.json`.
- **Схеми «де саме стає накладна деталь»** (клас `Scheme`) — для F_tower, D_clevis,
  H_clevis, bk_ear: беруться з `make_pos_drawing.compute()` (винесено з `main()` саме
  для цього; `main()` там — ручна перевірка з `positions.json`), малюються 1:1 у стилі
  аркуша, ставляться у вільне місце аркуша РАНІШЕ за рендери, а на сторінці кроків —
  завжди, у широкій картці поруч із рендером. Усі числа друковані. Нова накладна деталь
  = запис у `SPECS` там, у `make_pos_drawing.py`; аркуші підхоплять самі.
- **Про метал на аркушах — ані слова** (вимога користувача). Тексти кроків з
  `assembly.tsv` перераховуються `printed_text()`: `N мм (x друкованих)` → `x мм`,
  `Ø66` → `Ø13.2`, `R55` → `R11`, `N мм` → `N/5 мм`, порівняння пальців у дужках і
  «розділ «Де саме стають…»» прибираються. Коротка назва деталі без «300×100×12».
  Перевірка: `pdftotext … | grep -E 'друкованих|у металі|Ø66'` має мовчати.
- **Комірка рахує габарит разом з розмірами**; якщо вузол не вміщається на A4,
  генератор кладе решту на «продовження» і каже УВАГА — це не норма, а сигнал
  зменшити кількість розмірів чи відступи (`OFF`, `STEP`, `TXT` у caliper_dims).
- **Пакування** — MaxRects трьома евристиками × трьома порядками, береться розкладка
  з найбільшим суцільним вільним прямокутником (туди стають рендери). Аркуш стріли
  заповнений комірками на ~80 %, тому туди влазить один рендер, другий іде на сторінку
  кроків — це норма, а не помилка.
- **Рендери**: у `sheets.scad` i-та деталь зі списку `keys` фарбується в hue = i·360/n;
  зовнішній `color()` у прев'ю OpenSCAD перекриває внутрішні, тому модель не змінюється.
  Виноска ставиться на піксель того відтінку, **найближчий до центроїда** (сам центроїд
  у дзеркальної пари падає в порожнечу між половинками). Тло — колір кутового пікселя,
  сірі деталі кроків (S ≈ 0) у маски не потрапляють.
- **Ракурси не підбираються руками**: з 12 кандидатів (rotx 55/90/125 × rotz
  25/115/205/295) беруться за покриттям деталей у пікселях (перший — згори). Скільки —
  `RENDER_NVIEWS`: стріла/рукоять/ківш два (+ третій сам, якщо якусь деталь на двох видно
  менш ніж наполовину, або заради деталі з `RENDER_FOCUS` — ківш «зсередини» заради ребра
  губи), коромисло/тяга/циліндри по одному, колона — жодного (є картка 5.1). Так
  попросив користувач: прості вузли не потребують деталізації, другий ракурс лише
  дублює картку. Картка кроку — основний ракурс; якщо нову деталь на ньому майже не
  видно (< 400 px або < `STEP_MIN_SHARE` = 20 % від її найкращого огляду — права боковина
  ковша з лівого боку видна лише ребром), береться кандидат, де її видно найбільше. Деталь без виноски на
  жодному ракурсі → «УВАГА» у виводі.
- **Огляди — великими.** На аркуші розкладки огляд лишається, лише якщо виходить ≥ 100 мм
  завширшки; решта йдуть на сторінку кроків на всю ширину (≤ 118 мм заввишки, щоб два
  вмістились) — на них видно, як усе має бути у фіналі. Один і той самий огляд двічі
  (аркуш + сторінка кроків) — не робити, це вже прибирали.
- **Одноманітні кроки** (`STEP_GROUPS`: гільза + шток ×3) — одна широка картка з оглядом
  і переліком файлів замість шести карток.
- **Пальці на плані всієї машини — дві крапки** (40–60 px), тому в огляді вони
  потовщені (`fat=true` в обгортці), а картки кроків для них — **крупні плани
  шарнірів**: центри з `echo(PINPOS)` обгортки, `--camera=центр,кути,700` без `--viewall`.
- **1:1 тримається на CSS-міліметрах**: SVG `width="190mm"` + `viewBox` у мм, Chrome
  друкує з `preferCSSPageSize`. На кожному аркуші лінійка 100 мм — перевірка принтера.
  Перевірено числом: у прев'ю при 192 dpi лінія 190 мм = 1436 px.
- **PDF** робить `to_pdf` з `tools/html_pdf.py` (Chrome; номери сторінок —
  через `node` + `puppeteer-core` у `tools/media`). Нових залежностей не додано.

- **Логотип на аркушах** — той самий, що вийде з принтера: на контурі 1:1 малюється дно
  виїмки з STL (`brand_recess()`: грані вгору на `BRAND_DEPTH` нижче верху; площа — `logo_mm2`
  у `sheets.json`), на рендерах — наліпка моделі (`node_brand()` у `sheets.scad`, ПОЗА
  `color()` деталі, інакше знак перефарбується в її колір).
- **Кеш рендерів сам бачить зміни моделі**: у назві кожного рендера — хеш джерел (модель,
  `scad/brand/`, `parts.scad`, `brand.scad`, `sheets.scad`, див. `Renders.SOURCES`). `--fresh`
  потрібен лише коли змінилось щось поза цим списком (додав джерело — допиши його туди).

## Як розширювати

- **Нова деталь у наборі** → рядки в `parts.tsv` і `assembly.tsv`; більше нічого. Якщо
  вона кругла або будується в `parts.scad`, а не дістається з вузла через `$sel`, —
  перевір, що на рендері вузла її видно: `sheets.scad` малює деталь через
  `{ $sel = k; part_by_name(вузол); }`, тож у моделі має бути `sel("k")` саме з цим
  ключем (так уже є для всіх втулок і бобишок). Труби й плити колони мають окремі
  гілки в `node_part()`.
- **Новий вузол** → запис у `SHEETS` (slug латиницею, заголовок, підзаголовок, вузли з
  `assembly.tsv`, рендери), за потреби `RENDER_KEYS` (які ключі малює який рендер) і
  `node_of()` у `sheets.scad` (у якій системі координат моделі його брати).
- **Новий тип рендера** (як `pins` чи `cyl`) — гілка в `sheets.scad`; якщо йому потрібні
  змінні моделі (`show_ground` тощо) — лише через `-D` у `RENDER_EXTRA`: змінні моделі
  читаються з її власної області, з обгортки їх не перекрити.

## Граблі

- **Headless OpenSCAD зрідка падає мовчки** (rc ≠ 0, порожній stderr, файлу нема) — рендер
  повторюється до трьох разів; не «лікувати» це інакше.
- **`--view=edges` у headless прев'ю не малює нічого.** Межі між деталями домальовуються
  з міток пікселів (де мітка сусіда інша) — інакше на чорно-білому папері форма зникає.
- `!!! невідома деталь: none` модель каже завжди при `part="none"` — це не помилка,
  фільтрується. Справжні — `ERROR|WARNING:` і інші `!!!`.
- **Ширина тексту оцінюється** (≈ 0.55 em на знак), не міряється: довгий підпис робить
  комірку ширшою за контур. Скорочуй тексти (коротка назва обрізається до 34 знаків),
  а не «підганяй» коефіцієнт під один випадок.
- **Порожнє поле в TSV** зсуває колонки (таби злипаються) — «нема значення» = `-`;
  `read_tsv` падає, якщо колонок не стільки, скільки очікує.
- Не тримай кириличних назв у `png/`: README посилаються на них шляхом.

## Перевірка після змін — числом, потім одним оком

1. `--only N --no-steps --no-pdf --png` для ітерацій; повний запуск наприкінці.
2. Читай `sheets.json`: кожна комірка в межах `[0,190]×[0,268]`, без перекриттів
   (перевірка — десяток рядків на Python), `uncovered` порожній, у `steps` немає
   `px` < 300, `renders_overflow` лише там, де аркуш справді повний.
3. Стандартний вивід: «УВАГА» про деталь без виноски або крок, де деталь не видно.
4. Лише тоді — один погляд на `png/` того аркуша, який змінювався. Кожен перегляд
   картинки коштує як кілька сотень рядків коду.
